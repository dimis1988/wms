<div class="container">
<section class="jumbotron text-center">
<br>
        <div class="container">
          <h3 class="display-6">All Product Categories</h3>
          <p>Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
        </div>
      </section>
      <hr>
      <div class="album py-5 bg-light">
        <div class="container">

          <div class="row">

            <% @product_categories.each do |cat| %> 
            <div class="col-md-4">
              <div class="card mb-4 box-shadow">
              <% if cat.image.attached? %>
                <%= link_to image_tag(cat.image, class:"card-img-top"), cat %>
              <% end %>
                <div class="card-body">
                  <h5><%= link_to cat.name, cat %></h5>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group">
                      <%= link_to 'Details', cat, class:"btn btn-primary" %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
           <% end %>


          </div>
        </div>
      </div>

</div>