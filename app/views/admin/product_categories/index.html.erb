
<div class="container">
<section class="jumbotron text-center">
<br>
        <div class="container">
          <h3 class="display-6">All Product Categories</h3>
          <p>Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
        </div>
      </section>
      <hr>
      <%= link_to 'New Category', new_admin_product_category_path, class:"btn btn-primary btn-md" %>

<table class="table">
  <thead>
    <tr>
      <th scope="col">#id</th>
      <th scope="col">Name</th>
      <th scope="col"></th>
      <th scope="col">Actions</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    <% @product_categories.each do |cat| %>
    <tr>
      <th scope="row"><%= cat.id %></th>
      <td><%= link_to cat.name, admin_product_category_path(cat) %></td>
      <td><%= link_to "Details", admin_product_category_path(cat), class:"btn btn-primary btn-sm" %></td>
      <td><%= link_to "Edit", edit_admin_product_category_path(cat), class:"btn btn-primary btn-sm" %></td>
      <td><%= button_to "Delete", cat, method: :delete, class:"btn btn-danger btn-sm" %></td>
    </tr>
    <% end %>
  </tbody>
</table>



          </div>
        </div>
      </div>

</div>

