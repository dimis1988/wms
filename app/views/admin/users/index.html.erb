<div class="container">
<section class="jumbotron text-center">
<br>
        <div class="container">
          <h3 class="display-6">All Users</h3>
          <p>Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
        </div>
      </section>
      <hr>        
      <!--<%= link_to 'New Order', new_admin_order_path, class:"btn btn-primary btn-md" %>-->

<table class="table">
  <thead>
    <tr>
      <th scope="col">#id</th>
      <th scope="col">Full Name</th>
      <th scope="col">Email</th>
      <th scope="col">Admin</th>
      <th scope="col"></th>
      <th scope="col">Actions</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
    <tr>
      <th scope="row"><%= user.id %></th>
      <td><%= user.full_name %></td>
      <td><%= user.email %></td>
      <td>
        <% if user.admin %>
        <i class="fa fa-check" style="color: green;"></i>
            <% else %>
        <i class="fa fa-times" style="color: red;"></i>
      <% end %>
      </td>
      <td><%= link_to "Details", admin_user_path(user), class:"btn btn-primary btn-sm" %></td>
      <td><%= link_to "Edit", edit_admin_user_path(user), class:"btn btn-primary btn-sm" %></td>
      <td><%= button_to "Delete", admin_user_url(user), method: :delete, class:"btn btn-danger btn-sm" %></td>
    </tr>
    <% end %>
  </tbody>
</table>



          </div>
        </div>
      </div>

</div>

